# nyan-army-web

Animated Nyan Cat SVG using the original cat artwork, with a full-page (`100vh`) HTML stage where the animation runs from left to right.

## Run locally

```bash
python -m http.server 8000
```

Then open `http://localhost:8000`.
